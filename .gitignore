# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
*.local
tmp

# Transient artifacts written by the Discord request pipeline (workflow + local tests)
request.txt
prompt.txt
BLOCKED.txt

# Throwaway Phase 1 migration inputs (reproducible from sibling ffu-app repo).
# Deleted once per-game validation passes — see legacy-source/README.md.
legacy-source

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
