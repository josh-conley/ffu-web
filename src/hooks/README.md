# hooks

Thin React wrappers over the data provider (`useSeason`, `useSeasons`, `useDraft`, `useLineups`)
with caching. Components consume hooks + selectors; they never reach into the provider directly.
