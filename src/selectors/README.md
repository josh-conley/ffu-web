# selectors

Pure, memoized derivations over normalized data: ranking/tiebreakers, UPR, records, head-to-head,
career aggregates. **Derived data is never stored — always computed here.** Each piece of logic
lives in exactly one module and is unit-tested (Vitest) alongside the code.
